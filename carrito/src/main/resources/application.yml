server:
  port: 8082

spring:
  application:
    name: ms-carrito

  datasource:
    url: jdbc:mysql://localhost:3306/carrito?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: ""

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

url:
  auth: http://localhost:8080/auth/usuarios/
  catalogo: http://localhost:8081/catalogo/pizzas/

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    operationsSorter: method
    tagsSorter: alpha

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true